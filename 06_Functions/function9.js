/**
 * The new operator in JavaScript is used to create an instance of a user-defined object type or one of the built-in object types
 *  that has a constructor function.
 */
const d = new Date();
console.log(d);
console.log(d.getDate());
console.log(d.getDay());